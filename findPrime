T = int(raw_input())
for i in range(T):
    N = int(raw_input())
    if N == 1: print "Not prime"
    else :
        temp = []
        for j in range(1,int(N**0.5)):
            if N%j == 0 : temp.append(1)
        #print temp
        if len(temp) >= 2: print "Not prime"
        else: print "Prime"
